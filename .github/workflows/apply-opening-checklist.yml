name: Apply opening checklist
on:
  push:
    paths:
      - 'scripts/apply-opening-checklist.py'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply opening checklist
        run: python scripts/apply-opening-checklist.py
      - name: Commit update and remove updater
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git rm -f scripts/apply-opening-checklist.py .github/workflows/apply-opening-checklist.yml
          git commit -m "Replace opening checks with tick box checklist"
          git push